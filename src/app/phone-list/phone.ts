export class Phone {
  age: number;
  id: string;
  imageUrl: string;
  name: string;
  snippet: string;
  carrier?: string;
  description?: string;
  images?: string[];
  availability?: string[];
  battery?: object;
  storage?: object;
  connectivity?: object;
  android?: object;
  sizeAndWeight?: object;
  display?: object;
  hardware?: object;
  camera?: object;
}
